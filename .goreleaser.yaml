version: 2
before:
  hooks:
    - go mod tidy
builds:
  - env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    binary: cue
archives:
  - formats: [binary]
    name_template: "cue_{{ .Os }}_{{ .Arch }}"
release:
  github:
    owner: laeioun
    name: cue
